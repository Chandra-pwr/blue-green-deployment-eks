apiVersion: v1
kind: Service
metadata:
  name: bluegreen-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: my-app
    version: blue   # Initially pointing to BLUE
  ports:
    - port: 80
      targetPort: 3000